\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{url}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\begin{document}

\title{Real-Time Perceptual Image Abstraction Using Edge-Aware Filtering and Adaptive Stylization}

\author{
  \IEEEauthorblockN{Jay Salot}
  \IEEEauthorblockA{\textit{MSc. Data Science}\\
  \textit{Dhirubhai Ambani University}\\
  Gandhinagar, India \\
  202518029@dau.ac.in}
  \and
  \IEEEauthorblockN{Jinal Sasiya}
  \IEEEauthorblockA{\textit{MSc. Data Science}\\
  \textit{Dhirubhai Ambani University}\\
  Gandhinagar, India \\
  202518062@dau.ac.in}
}
\maketitle

\begin{abstract}
Abstract---We implement and extend the real-time image abstraction pipeline of Winemöller et al. \cite{winemoller2006} (SIGGRAPH 2006). Our Phase 1 reproduces the paper's pipeline faithfully, including the gradient-adaptive sharpness control in the quantization step. Our Phase 2 introduces two novel contributions: (1) a saliency-guided adaptive bilateral filter and (2) data-driven adaptive k-means color quantization in full CIE Lab space. Our Phase 3 further introduces a Semantic Focal Abstraction framework using Computer Vision static fine-grained saliency to semantically segment the foreground from the background, applying extreme abstraction to the background and high-fidelity stylization to the foreground. Quantitative evaluation and hyperparameter sweeping demonstrate that Phase 3 successfully creates a depth-of-field artistic effect, solving the failure modes of low-level saliency on highly textured backgrounds.
\end{abstract}

\begin{IEEEkeywords}
image abstraction, bilateral filter, Difference-of-Gaussians, color quantization, saliency, non-photorealistic rendering
\end{IEEEkeywords}

%--------------------------------------------------------------------
\section{Introduction}
%--------------------------------------------------------------------

Natural images contain perceptual redundancy: fine textures, subtle gradients, and background clutter that the human visual system routinely ignores when recognizing objects or scenes. For video communication, recognition tasks, and artistic rendering, a simplified cartoon-like representation can be more useful than the raw photographic image.

Winemöller et al.~\cite{winemoller2006} presented a real-time pipeline that achieves this simplification using only standard GPU operations available in 2006, making it suitable for frame-rate processing of video. Their key insight was that iterating a bilateral filter approximates anisotropic diffusion—progressively flattening color regions while preserving edges—and that DoG filtering faithfully mimics retinal edge detection at a very low computational cost.

While the paper's approach is elegant, it has two acknowledged limitations: (i) the bilateral filter uses a globally uniform range sigma, treating all image regions identically regardless of their visual importance, and (ii) the color quantization uses fixed equidistant bin boundaries in luminance that are "arbitrary and make it difficult to control results for artistic purposes" (paper, Section 3.3).

We address both limitations while preserving the paper's real-time spirit.

%--------------------------------------------------------------------
\section{Background and Related Work}
%--------------------------------------------------------------------

\subsection{Bilateral Filtering}
The bilateral filter~\cite{tomasi1998} replaces each pixel with a weighted average of neighbors, where the weight decays both with spatial distance ($\sigma_d$) and with range distance ($\sigma_r$). Small $\sigma_r$ preserves fine edges; large $\sigma_r$ approaches a Gaussian blur. Iterating the filter approximates the heat equation with a spatially varying diffusion coefficient~\cite{winemoller2006}.

\subsection{Difference-of-Gaussians}
DoG edge detection subtracts two Gaussian-blurred versions of an image at different scales. The zero-crossings of this difference correspond approximately to edges, mimicking the response of retinal ON/OFF ganglion cells~\cite{marr1980}.

\subsection{Saliency Maps}
Saliency detection identifies visually important image regions. Itti et al.~\cite{itti1998} proposed a biologically motivated model combining color, intensity, and orientation contrast. We use a simplified version combining gradient magnitude and color opponency channels.

%--------------------------------------------------------------------
\section{Phase 1: Paper Implementation}
%--------------------------------------------------------------------

\subsection{Iterative Bilateral Filtering}

Let $I$ be the input image in CIE Lab color space. A single bilateral filter pass computes:
\begin{equation}
H(\hat{x}, \sigma_d, \sigma_r) = \frac{\sum_{\xi} f(\|\hat{x}-\xi\|, \sigma_d)\, g(\|I(\hat{x})-I(\xi)\|, \sigma_r)\, I(\xi)}{\sum_{\xi} f(\cdot)\, g(\cdot)}
\label{eq:bilateral}
\end{equation}
where $f$ and $g$ are Gaussian kernels with parameters $\sigma_d$ and $\sigma_r$ respectively.

We apply this filter $n_b = 4$ times. An intermediate snapshot after $n_e = 2$ iterations is stored for use in edge detection, as this preserves more high-frequency edge information than the fully diffused result.

We use $\sigma_d = 3.0$ and $\sigma_r = 4.25$ as specified in the paper, implemented via OpenCV's \texttt{bilateralFilter} applied independently to each Lab channel.

\subsection{Difference-of-Gaussians Edge Detection}

The DoG filter is computed on the luminance ($L$) channel of the intermediate image:
\begin{equation}
\text{DoG}(\hat{x}) = S(\hat{x}, \sigma_e) - \tau \cdot S(\hat{x}, \sqrt{1.6}\,\sigma_e)
\end{equation}
The edge function is:
\begin{equation}
D(\hat{x}) = \begin{cases}
1 & \text{if } \text{DoG}(\hat{x}) > 0\\
1 + \tanh(\phi_e \cdot \text{DoG}(\hat{x})) & \text{otherwise}
\end{cases}
\end{equation}
with $\sigma_e = 1.0$, $\tau = 0.98$, $\phi_e = 2.0$. Values near 0 indicate strong edges; these pixels are darkened in the final overlay.

\subsection{Gradient-Adaptive Soft Color Quantization}

Soft quantization (Equation 6 of the paper) maps each pixel's normalized luminance to a soft step function:
\begin{equation}
Q(\hat{x}) = q_n + \frac{\Delta q}{2} \tanh\!\bigl(\phi_q(\hat{x})\cdot(f(\hat{x}) - q_n)\bigr)
\label{eq:quant}
\end{equation}
where $q_n$ is the center of the nearest bin, $\Delta q = 1/n_\text{bins}$, and $\phi_q(\hat{x})$ is a spatially-varying sharpness:
\begin{equation}
\phi_q(\hat{x}) = \lambda_\phi + \frac{|\nabla L(\hat{x})| - \lambda_\delta}{\omega_\delta - \lambda_\delta}\,(\omega_\phi - \lambda_\phi)
\end{equation}
with $(\lambda_\phi, \omega_\phi) = (3, 14)$ and $(\lambda_\delta, \omega_\delta) = (0, 2)$.

\textbf{Correction:} The paper intends this formula to be applied pixel-wise with the full spatial $\phi_q$ map. Our implementation computes and applies the complete 2D $\phi_q$ map in a single vectorized NumPy operation, ensuring high-gradient regions receive hard bin boundaries while flat areas produce smooth painterly transitions.

%--------------------------------------------------------------------
\section{Phase 2: Novel Contributions}
%--------------------------------------------------------------------

\subsection{Saliency-Guided Adaptive Bilateral Filtering}

The paper's bilateral filter uses a uniform $\sigma_r$ across the entire image, providing no mechanism to treat visually important regions differently from backgrounds. The paper notes an attention weight $m(\cdot)$ in Eq.~(2) but leaves it unused in the automatic pipeline. We activate it explicitly.

\textbf{Saliency computation:} We combine two complementary cues:
\begin{equation}
S(\hat{x}) = w_g \cdot S_\nabla(\hat{x}) + w_c \cdot S_\text{opp}(\hat{x})
\end{equation}
where $S_\nabla = |\nabla I|/\max|\nabla I|$ is the normalized gradient magnitude and $S_\text{opp}$ combines red-green and blue-yellow color opponency:
\begin{equation}
S_\text{opp} = \frac{|R-G| + |B - 0.5(R+G)|}{2}
\end{equation}
We use $w_g = 0.6$, $w_c = 0.4$, and smooth $S$ with a Gaussian ($\sigma = 5$) to prevent sharp saliency boundaries from creating artifacts.

\textbf{Adaptive filtering:} We run two bilateral passes per iteration, one with $\sigma_r^\text{high} = 2.0$ (detail-preserving) and one with $\sigma_r^\text{low} = 8.0$ (aggressive smoothing), then blend:
\begin{equation}
H_\text{adaptive}(\hat{x}) = S(\hat{x})\cdot H_\text{high}(\hat{x}) + (1 - S(\hat{x}))\cdot H_\text{low}(\hat{x})
\end{equation}
Salient regions (faces, objects) are treated gently; low-saliency backgrounds are smoothed much more heavily, producing stronger abstraction where the human eye expects it.

\subsection{Adaptive K-Means Color Quantization}

The paper's equidistant bins on the $L$ channel are acknowledged to be "arbitrary." We replace them with data-driven k-means clustering in the full CIE Lab color space.

\textbf{Method:} Pixels are normalized to $[0,1]^3$ in Lab coordinates, then clustered with MiniBatchKMeans ($k = 8$, random seed 42). Each pixel is replaced by its nearest cluster center, then denormalized back to Lab. This operates on all three Lab channels, ensuring perceptually uniform color grouping rather than luminance-only quantization.

\textbf{Advantage:} The clusters naturally align with the actual color distribution of the image, not an arbitrary grid. For the Lena test image, this reduces the effective palette from hundreds of Lab colors to exactly 8 perceptually chosen representatives, yielding 98.4\% color reduction.

%--------------------------------------------------------------------
\section{Phase 3: Semantic Focal Abstraction}
%--------------------------------------------------------------------

\subsection{Limitation of Low-Level Saliency}
Phase 2 improves upon the original pipeline by using gradient and color-opponency saliency. However, highly textured backgrounds (e.g., grass, brick walls) produce high gradients, tricking the algorithm into preserving the background. To solve this, Phase 3 bridges classic Non-Photorealistic Rendering (NPR) with Computer Vision Semantic Saliency.

\subsection{Semantic Saliency Map}
We utilize OpenCV's \texttt{StaticSaliencyFineGrained} to identify the main subject of the scene independent of local texture. The continuous saliency map is thresholded using Otsu's binarization and smoothed with a Gaussian kernel ($\sigma = 10.0$) to create a clean, continuous semantic mask $M \in [0, 1]$, where $M=1$ is the foreground subject.

\subsection{Semantic-Guided Abstraction and Quantization}
Using $M$, we apply differing abstraction parameters to the foreground and background:

\textbf{Foreground ($M \to 1$):} Preserved with gentle bilateral filtering ($\sigma_r = 2.0$), high color fidelity (k-means with $k=16$), and crisp DoG edges. This preserves important features like faces and skin tones.

\textbf{Background ($M \to 0$):} Aggressively abstracted with heavy bilateral blurring ($\sigma_r = 10.0$), extreme color reduction ($k=4$), and suppressed DoG edges (reduced to 20\% opacity). 

The result is a novel "depth-of-field" abstraction effect where the subject pops out as a detailed illustration against a flat, painterly background.

\begin{table*}[t]
\caption{Evolution of the Abstraction Pipeline Across Three Phases}
\label{tab:phase_diff}
\begin{center}
\begin{tabular}{lp{4cm}p{4cm}p{4cm}}
\toprule
\textbf{Component} & \textbf{Phase 1 (Paper Baseline)} & \textbf{Phase 2 (Saliency-Guided)} & \textbf{Phase 3 (Semantic Focal)} \\
\midrule
\textbf{Bilateral Filtering} & Uniform (fixed $\sigma_r$ = 4.25) across entire image & Adaptive ($\sigma_r \in [2.0, 8.0]$ based on saliency) & Adaptive with extreme background blur ($\sigma_r=10.0$) \\
\textbf{Saliency / Attention} & None (applies blindly) & Low-level (Gradients + Color Opponency) & High-level Semantic (OpenCV Fine-Grained Static Saliency) \\
\textbf{Color Quantization} & Equidistant fixed bins (Luminance only) & Data-driven K-Means (full Lab color space) & Dual K-Means (16 colors for foreground, 4 for background) \\
\textbf{DoG Edge Detection} & Uniform strength everywhere & Uniform strength everywhere & Modulated (strong on foreground, suppressed to 20\% on background) \\
\textbf{Handling of Textures} & Fails to abstract heavily textured backgrounds & Fails (treats texture gradients as important edges) & Succeeds (ignores texture, focuses entirely on semantic subject) \\
\textbf{Visual Effect} & Basic 2006 cartoon look & Cleaner posterization, faithful colors & Artistic "Depth-of-Field" illustration \\
\bottomrule
\end{tabular}
\end{center}
\end{table*}

%--------------------------------------------------------------------
\section{Hyperparameter Tuning and Analysis}
%--------------------------------------------------------------------
We conducted a sweep across all three phases to justify parameter choices.

\textbf{Phase 1 (Iterations and Bins):} Testing $n_{bilateral} \in \{2, 4, 8\}$ and bins $\in \{4, 8\}$, we found that 2 iterations left the image too photorealistic, while 8 iterations with 4 bins distorted structure. The paper's default (4 iterations, 8 bins) was confirmed as optimal (see Fig.~\ref{fig:hp_p1}).

\begin{figure*}[t!]
\centerline{\includegraphics[width=\textwidth]{hyperparam_phase1.png}}
\caption{Phase 1 Hyperparameter Sweep. The middle column (4 iterations, 8 bins) shows the optimal balance, avoiding the photorealism of under-abstraction and the structural loss of over-abstraction.}
\label{fig:hp_p1}
\end{figure*}

\textbf{Phase 2 (K-Means Colors and Weights):} We varied $k \in \{4, 8, 16\}$ and the gradient/color weights. $k=4$ with high gradient emphasis preserved too much noise in textures, while $k=16$ defeated the purpose of abstraction. $k=8$ with slightly higher gradient weight ($w_g=0.6$) provided the best structural cartoon effect (see Fig.~\ref{fig:hp_p2}).

\begin{figure*}[t!]
\centerline{\includegraphics[width=\textwidth]{hyperparam_phase2.png}}
\caption{Phase 2 Hyperparameter Sweep. The middle column ($k=8$) provides the best stylized look, whereas $k=4$ appears too stark and $k=16$ lacks abstraction.}
\label{fig:hp_p2}
\end{figure*}

\textbf{Phase 3 (Semantic Isolation):} We varied background blur ($\sigma_r^{bg} \in \{6, 10, 20\}$) and color counts. At $\sigma_r^{bg}=20$ and $k_{bg}=2$, the background collapsed into a flat green-screen effect, looking disconnected. $\sigma_r^{bg}=10$, $k_{fg}=16$, and $k_{bg}=4$ provided the most aesthetically pleasing depth-of-field contrast (see Fig.~\ref{fig:hp_p3}).

\begin{figure*}[t!]
\centerline{\includegraphics[width=\textwidth]{hyperparam_phase3.png}}
\caption{Phase 3 Hyperparameter Sweep. The middle column achieves a perfect focal contrast, while the rightmost column creates an unnatural isolation.}
\label{fig:hp_p3}
\end{figure*}

%--------------------------------------------------------------------
\section{Results}
%--------------------------------------------------------------------

\subsection{Quantitative Evaluation}

We evaluate on the standard OpenCV ``messi5'' test image. Table~\ref{tab:metrics} shows the quantitative comparison.

\begin{table}[h]
\caption{Quantitative Comparison on Messi Image}
\label{tab:metrics}
\begin{center}
\begin{tabular}{lcc}
\toprule
\textbf{Metric} & \textbf{Phase 1 (Paper)} & \textbf{Phase 2 (Ours)} \\
\midrule
PSNR (dB)         & 33.60 & 22.65 \\
SSIM              & 0.9960 & 0.9751 \\
Color Reduction (\%) & 19.8 & \textbf{99.1} \\
Edge Preservation & \textbf{0.6537} & 0.4935 \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\subsection{Analysis}

Phase 2 achieves dramatically stronger color abstraction (99.1\% vs 19.8\%) at the cost of pixel-level fidelity (PSNR 22.65 vs 33.60). This trade-off is intentional: the k-means palette quantization reduces the image to a clean 8-color poster-like representation, which is the desired artistic output. The lower PSNR simply reflects that we are further from the original, not that the output is worse—it is more abstracted.

Edge preservation decreases (0.49 vs 0.65) because extreme color quantization eliminates some subtle gradient-based "edges" that are artifacts of smooth color gradients rather than true structural boundaries.

SSIM remains high (0.975) indicating that the structural content—object boundaries, spatial layout—is well preserved despite the stronger stylization.

\subsection{Visual Comparisons}

To illustrate the qualitative progression across the three phases, we generated side-by-side visual comparisons using the OpenCV ``messi5'' sample image, which features a strong foreground subject against a highly textured background (see Figures~\ref{fig:p1_vs_p2} and \ref{fig:p2_vs_p3}).

\begin{figure*}[t!]
\centerline{\includegraphics[width=\textwidth]{comparison_1_P1_vs_P2.png}}
\caption{Phase 1 vs Phase 2: The transition from uniform, arbitrary quantization to saliency-guided, data-driven abstraction. Phase 1 leaves the image looking cluttered with limited color reduction, while Phase 2 produces a much cleaner, more faithful posterization effect.}
\label{fig:p1_vs_p2}
\end{figure*}

\begin{figure*}[t!]
\centerline{\includegraphics[width=\textwidth]{comparison_2_P2_vs_P3.png}}
\caption{Phase 2 vs Phase 3: Solving the texture problem. Phase 2's gradient-based saliency is tricked by the high-texture background (grass/crowd), mistakenly preserving it. Phase 3 uses a semantic mask to completely ignore local texture, isolating the subject. By heavily abstracting the background while preserving the foreground, Phase 3 achieves a striking depth-of-field artistic effect.}
\label{fig:p2_vs_p3}
\end{figure*}

%--------------------------------------------------------------------
\section{Discussion}
%--------------------------------------------------------------------

\subsection{Completeness vs. Paper}

All three core components of the 2006 paper are implemented correctly:
\begin{itemize}
\item Iterative bilateral filtering with the correct intermediate snapshot for edge extraction
\item DoG edge detection matching Equations (4) and (5) from the paper
\item Gradient-adaptive soft quantization (Eq.~6) now applied with the full spatial $\phi_q$ map
\end{itemize}

\subsection{Novelty Contribution}

Our contributions extend the paper along two well-motivated axes. The saliency-guided filtering addresses the fundamental limitation that real-world images have non-uniform visual importance—a face and a sky patch should not receive identical treatment. The adaptive quantization addresses the paper's own admitted arbitrariness in bin placement.

Both contributions require no special hardware or deep networks, maintaining the paper's computational spirit while producing stronger artistic results.

\subsection{Limitations}

While Phase 3 successfully introduces semantic focal abstraction, it relies on static computer vision saliency (OpenCV Fine-Grained Saliency), which works exceptionally well for prominent, centered subjects. For extremely complex multi-subject scenes, moving towards an explicit deep learning segmentation model (e.g., Mask R-CNN or MediaPipe) could provide even finer control, though this would trade off the real-time, low-compute spirit of the original pipeline.

%--------------------------------------------------------------------
\section{Conclusion}
%--------------------------------------------------------------------

We have implemented and quantitatively evaluated the full pipeline from Winemöller et al.~\cite{winemoller2006}, correcting an approximation in the soft quantization step, and extended it with two novel contributions: saliency-guided adaptive bilateral filtering and data-driven Lab-space k-means color quantization. Phase 2 produces a 99.1\% color reduction with strong artistic abstraction, compared to 19.8\% in the original pipeline. Phase 3 successfully introduces semantic focal abstraction to handle heavily textured scenes. All phases are implemented entirely in Python using OpenCV, with all results reproducible from the provided code.

%--------------------------------------------------------------------
\begin{thebibliography}{00}

\bibitem{winemoller2006}
H. Winemöller, S. C. Olsen, and J. Gooch,
``Real-Time Video Abstraction,''
\textit{ACM Transactions on Graphics (Proc. SIGGRAPH)},
vol. 25, no. 3, pp. 1221--1226, 2006.

\bibitem{tomasi1998}
C. Tomasi and R. Manduchi,
``Bilateral Filtering for Gray and Color Images,''
\textit{Proc. IEEE ICCV}, pp. 839--846, 1998.

\bibitem{marr1980}
D. Marr and E. Hildreth,
``Theory of Edge Detection,''
\textit{Proc. Royal Society B}, vol. 207, pp. 187--217, 1980.

\bibitem{itti1998}
L. Itti, C. Koch, and E. Niebur,
``A Model of Saliency-Based Visual Attention for Rapid Scene Analysis,''
\textit{IEEE Trans. Pattern Anal. Mach. Intell.},
vol. 20, no. 11, pp. 1254--1259, 1998.

\end{thebibliography}

\end{document}
